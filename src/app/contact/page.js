import ContactPage from '@/components/ContactPage';

export const metadata = {
    title: 'Contact Us | Vilesh',
    description: 'Get in touch with us.',
};

export default function Page() {
    return <ContactPage />;
}
